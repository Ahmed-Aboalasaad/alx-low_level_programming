#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers
 *
 * @x: first number
 * @y: second number
 * Return: returns the sum of the given integers
 */
int add(int x, int y)
{
	int sum = x + y;

	return (sum);
}
