#include <stdio.h>
#include "main.h"

/**
 * main - starting point
 *
 * @argc: counts arguments
 * @argv: arguments vector
 * Return: 0 when succeeds
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
